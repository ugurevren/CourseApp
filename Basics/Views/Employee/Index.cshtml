@model List<string>
<div>
    <h3 class = "display-3">
        Employee
    </h3>
    <div class = "lead">
        @foreach (string message in Model ){
            <p> 
                @message
            </p>
            }
    </div> 
</div>